import { Box } from '@mui/material';

import CardItemContainer from './CardItemContainer';
const Feed = () => {
	return (
		<Box bgcolor="" flex={4} p={2}>
			<CardItemContainer />
		</Box>
	);
};

export default Feed;
